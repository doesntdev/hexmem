{
  "name": "hexmem",
  "version": "2.0.0",
  "description": "Multi-agent agentic memory system — PostgreSQL + pgvector",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "bin": {
    "hexmem": "./src/cli.ts"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc",
    "cli": "tsx src/cli.ts",
    "migrate": "tsx src/db/migrate.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.0",
    "openai": "^4.77.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}